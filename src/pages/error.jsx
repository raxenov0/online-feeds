import React from "react";

export const Error = ( ) =>{
    return(
        <div style={{color:'red', fontSize:'50px', fontWeight:'600', justifyContent:'center',display:'flex', alignItems:'center', height:'100vh'}}>
            Error
        </div>
    )
}